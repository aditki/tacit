# App-only compose file.
#
# For the full local demo stack with Grafana, Prometheus, and fake metrics, use:
#   docker compose -f docker-compose.dev.yml up -d
#
# The dev stack intentionally uses unsafe Grafana demo defaults and must stay
# local-only. This file does not start Grafana with anonymous Editor access.

services:
  tacit:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      API_AUTH_ENABLED: "true"
      API_AUTH_KEY: "${API_AUTH_KEY:?Set API_AUTH_KEY to a strong runtime secret}"
      API_ALLOWED_HOSTS: "${API_ALLOWED_HOSTS:-localhost,127.0.0.1}"
      LEARNED_ARCHETYPES_GENERATION_ENABLED: "false"
      LEARNED_ARCHETYPES_AUTOMATIC_REGISTRATION_ENABLED: "false"
      LEARNED_ARCHETYPES_NORMAL_RETRIEVAL_ENABLED: "false"
      LEARNED_ARCHETYPES_RETRIEVAL_MODE: curated_only
      LEARNED_ARCHETYPES_QUARANTINE_PATH: /app/data/generated_archetypes/quarantine
    volumes:
      - tacit_data:/app/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

volumes:
  tacit_data:
