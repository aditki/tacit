## Summary

Describe the user-visible and architectural change.

## Foundation Matrix

- Foundations touched:
- Matrix rows covered:
- Tests written before implementation:
- No-side-effect assertions:
- Concurrency/fault cases:
- Cross-runtime lifecycle gate (owner/controller/cancellation/cleanup), or N/A:
- Scaling bounds and query plans:
- Quality gates run:
- Whole-diff architecture/security reviews:
- Explicit exclusions with rationale:
- Remaining risks and observability gaps:

Use `Not applicable` with a reason instead of omitting a field. See
`docs/foundation-invariant-matrix.md`.

## Verification

List the exact checks and benchmarks run.
